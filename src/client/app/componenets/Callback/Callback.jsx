import React, { Component } from 'react';

export default class Callback extends Component {
    render() {
        console.log(this);
        return (
            <div>
                Loading...
            </div>
        );
    }
}